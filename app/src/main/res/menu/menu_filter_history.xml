<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Section: Types -->
    <group android:checkableBehavior="all">
        <item
            android:id="@+id/filter_expired"
            android:title="Expired"
            android:checkable="true"
            android:checked="true" />
        <item
            android:id="@+id/filter_consumed"
            android:title="Consumed"
            android:checkable="true"
            android:checked="true" />
        <item
            android:id="@+id/filter_deleted"
            android:title="Deleted"
            android:checkable="true"
            android:checked="true" />
    </group>

    <!-- Divider -->
    <item
        android:id="@+id/divider"
        android:title="──────────"
        android:enabled="false" />

    <!-- Section: Favourites -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/filter_favourites"
            android:title="Only Favourites"
            android:checkable="true"
            android:checked="false" />
    </group>

</menu>
